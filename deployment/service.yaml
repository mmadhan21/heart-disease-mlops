apiVersion: v1
kind: Service
metadata:
  name: heart-disease-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: heart-disease-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
